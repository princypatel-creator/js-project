# TODO
- [ ] Update `gate-pass.js` to render localStorage values into a table layout instead of stacked div lines.
- [ ] Update `gate-pass.html` styles (if needed) to support a readable table.
- [ ] Ensure missing values display as `—` and that page still shows a fallback message when no data exists.
- [ ] Test by loading `index.html`, clicking Save, then opening `gate-pass.html` to verify the table populates.
